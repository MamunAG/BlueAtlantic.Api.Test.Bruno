meta {
  name: Get an inventory category setting by id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/inventory-categories/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
