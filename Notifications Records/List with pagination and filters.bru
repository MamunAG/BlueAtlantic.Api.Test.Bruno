meta {
  name: List with pagination and filters
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/notification-records
  body: none
  auth: bearer
}

params:query {
  ~notificationType: Both
  ~notificationTemplateId: 2
  ~recipientType: Trainers Only
  ~page: 1
  ~limit: 10
}

auth:bearer {
  token: {{token}}
}
