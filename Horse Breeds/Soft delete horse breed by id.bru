meta {
  name: Soft delete horse breed by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/horse-breeds/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
