meta {
  name: Permanently delete workout session by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/workout-sessions/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 4
}

auth:bearer {
  token: {{token}}
}
