meta {
  name: Get workout session by id
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/workout-sessions/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
