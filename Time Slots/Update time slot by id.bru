meta {
  name: Update time slot by id
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/v1/time-slots/:id
  body: json
  auth: bearer
}

params:path {
  id: 16
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "timeOfDay": "09:00:00",
    "durationMinutes": 60,
    "serviceType": "workout",
    "status": "available"
  }
}
