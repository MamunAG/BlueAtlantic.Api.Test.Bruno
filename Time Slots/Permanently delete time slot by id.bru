meta {
  name: Permanently delete time slot by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/time-slots/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 16
}

auth:bearer {
  token: {{token}}
}
