meta {
  name: List time slots with pagination and filters
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/time-slots?status=active&serviceType=physio
  body: none
  auth: bearer
}

params:query {
  status: active
  serviceType: physio
  ~page: 
  ~limit: 
  ~timeOfDay: 
  ~minDuration: 
  ~maxDuration: 
}

auth:bearer {
  token: {{token}}
}
