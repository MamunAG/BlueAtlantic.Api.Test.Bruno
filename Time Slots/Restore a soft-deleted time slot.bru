meta {
  name: Restore a soft-deleted time slot
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/time-slots/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 17
}

auth:bearer {
  token: {{token}}
}
