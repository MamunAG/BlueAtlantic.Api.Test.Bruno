meta {
  name: Soft delete time slot by id
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/time-slots/:id
  body: none
  auth: bearer
}

params:path {
  id: 17
}

auth:bearer {
  token: {{token}}
}
