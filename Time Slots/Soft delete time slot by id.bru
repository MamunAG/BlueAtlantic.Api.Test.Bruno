meta {
  name: Soft delete time slot by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/time-slots/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
