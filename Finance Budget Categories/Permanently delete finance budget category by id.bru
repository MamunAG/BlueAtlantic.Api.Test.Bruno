meta {
  name: Permanently delete finance budget category by id
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/api/v1/finance-budget-categories/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
