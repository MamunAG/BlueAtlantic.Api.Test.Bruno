meta {
  name: Soft delete finance budget category by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/finance-budget-categories/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
