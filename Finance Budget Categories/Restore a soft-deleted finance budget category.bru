meta {
  name: Restore a soft-deleted finance budget category
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/api/v1/finance-budget-categories/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
