meta {
  name: Permanently delete horse health record by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/horse-health/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
