meta {
  name: Soft delete horse health record by id
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/horse-health/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
