meta {
  name: Approve expense by ID
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/api/v1/expenses/:id/approve
  body: none
  auth: bearer
}

params:path {
  id: 4
}

auth:bearer {
  token: {{token}}
}
