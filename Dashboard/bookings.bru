meta {
  name: bookings
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/dashboard/bookings?bookingType=Yearly
  body: none
  auth: bearer
}

params:query {
  bookingType: Yearly
  ~bookingType: Monthly
  ~bookingType: Weekly
}

auth:bearer {
  token: {{token}}
}
