meta {
  name: Reject expense by ID
  type: http
  seq: 10
}

patch {
  url: {{baseUrl}}/api/v1/expenses/:id/reject
  body: none
  auth: bearer
}

params:path {
  id: 4
}

auth:bearer {
  token: {{token}}
}
