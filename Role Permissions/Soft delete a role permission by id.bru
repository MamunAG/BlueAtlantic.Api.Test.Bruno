meta {
  name: Soft delete a role permission by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/role-permissions/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
