meta {
  name: Get all role permissions with pagination and filters
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/role-permissions
  body: none
  auth: bearer
}

params:query {
  ~page: 
  ~limit: 
  ~roleId: 
  ~resourceId: 
}

auth:bearer {
  token: {{token}}
}
