meta {
  name: Permanently delete a role permission by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/role-permissions/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
