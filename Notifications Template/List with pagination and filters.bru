meta {
  name: List with pagination and filters
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/notification-template?page=1&limit=10
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 10
  ~status: active
  ~name: 
  ~search: 
  ~minDuration: 
  ~maxDuration: 
}

auth:bearer {
  token: {{token}}
}
