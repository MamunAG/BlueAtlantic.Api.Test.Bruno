meta {
  name: Permanently delete
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/notification-template/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{token}}
}
