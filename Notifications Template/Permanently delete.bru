meta {
  name: Permanently delete
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/workout-types/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 7
}

auth:bearer {
  token: {{token}}
}
