meta {
  name: Update a media file metadata
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/v1/media-library/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {}
}
