meta {
  name: Permanently delete horse by id
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/horses/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 4
}

auth:bearer {
  token: {{token}}
}
