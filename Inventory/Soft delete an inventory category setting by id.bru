meta {
  name: Soft delete an inventory category setting by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/inventory/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
