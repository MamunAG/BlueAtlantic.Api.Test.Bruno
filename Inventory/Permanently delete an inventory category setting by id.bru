meta {
  name: Permanently delete an inventory category setting by id
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/inventory/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{token}}
}
