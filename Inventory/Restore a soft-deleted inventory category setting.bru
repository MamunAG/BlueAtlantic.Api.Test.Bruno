meta {
  name: Restore a soft-deleted inventory category setting
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/inventory/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 2
}

auth:bearer {
  token: {{token}}
}
