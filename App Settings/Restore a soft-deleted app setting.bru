meta {
  name: Restore a soft-deleted app setting
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/v1/app-settings/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
