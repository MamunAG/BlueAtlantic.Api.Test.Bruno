meta {
  name: Permanently delete app setting by id
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/api/v1/app-settings/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
