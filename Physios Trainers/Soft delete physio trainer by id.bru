meta {
  name: Soft delete physio trainer by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/physio-trainers/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
