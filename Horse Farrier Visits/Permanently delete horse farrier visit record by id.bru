meta {
  name: Permanently delete horse farrier visit record by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/horse-farrier-visits/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
