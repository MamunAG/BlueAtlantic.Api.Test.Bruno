meta {
  name: List feed types with pagination and filters
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/feed-types
  body: none
  auth: bearer
}

params:query {
  ~page: 
  ~limit: 
  ~status: 
  ~name: 
  ~search: 
}

auth:bearer {
  token: {{token}}
}
