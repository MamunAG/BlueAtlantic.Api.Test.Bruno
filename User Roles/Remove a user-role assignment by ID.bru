meta {
  name: Remove a user-role assignment by ID
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/user-roles/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
