meta {
  name: Permanently delete a user by id
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/users/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
