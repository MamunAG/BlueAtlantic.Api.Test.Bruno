meta {
  name: Soft delete a user by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/users/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
