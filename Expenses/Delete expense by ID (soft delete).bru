meta {
  name: Delete expense by ID (soft delete)
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/expenses/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
