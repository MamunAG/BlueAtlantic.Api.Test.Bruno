meta {
  name: Reject expense by ID
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/api/v1/expenses/:id/reject
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
