Perfect ✅ — you want your Git auto-sync script to run **on Windows startup**, **every 1 hour**, and **on shutdown**. I’ll give you a **complete step-by-step setup** for Task Scheduler in Windows 11.

We’ll assume your script is located in your project root, e.g.:

```
D:\Projects\BlueAtlantic.Api.Test.Bruno\auto_git_update.bat
```

---

## **Step 1: Open Task Scheduler**

* Press **Win + R**, type:

```
taskschd.msc
```

* Press **Enter**

---

## **Step 2: Create a New Task**

1. In Task Scheduler, click **Create Task…** (not Basic Task)
2. On the **General** tab:

   * Name: `Git Auto Sync`
   * Check **Run with highest privileges**
   * Configure for: **Windows 11**
   * Check **Run whether user is logged on or not**

---

## **Step 3: Add Triggers**

### 3a. Trigger for Windows Startup

* Go to **Triggers → New…**
* Begin the task: **At startup**
* Click **OK**

### 3b. Trigger for Every 1 Hour

* Click **New…** again
* Begin the task: **On a schedule → Daily**
* Repeat task every: **1 hour**
* For a duration: **Indefinitely**
* Click **OK**

### 3c. Trigger for Shutdown

* Click **New…**
* Begin the task: **On an event**
* Log: `System`
* Source: `User32`
* Event ID: `1074` (this triggers on user-initiated shutdown/restart)
* Click **OK**

---

## **Step 4: Add Action**

* Go to **Actions → New…**
* Action: **Start a program**
* Program/script:

```
cmd.exe
```

* Add arguments:

```
/c "D:\Projects\BlueAtlantic.Api.Test.Bruno\auto_git_update.bat"
```

* Start in (optional):

```
D:\Projects\BlueAtlantic.Api.Test.Bruno
```

* Click **OK**

---

## **Step 5: Conditions Tab**

* Uncheck: **Start the task only if the computer is on AC power**
* Optional: Uncheck **Stop if computer switches to battery power**

---

## **Step 6: Settings Tab**

* Check:

  * **Allow task to be run on demand**
  * **Run task as soon as possible after a scheduled start is missed**
* Optional: **If task fails, restart every 1 minute up to 3 times**

---

## **Step 7: Test the Task**

* Right-click the task → **Run**
* Check `git_auto_sync_log.txt` in your project root for logs.
* The task will now run:

  1. At Windows startup
  2. Every 1 hour
  3. On shutdown

---

💡 **Tip:**
Make sure your `.gitignore` has `git_auto_sync_log.txt` so the log file doesn’t get committed.

---

If you want, I can also make a **ready-to-import Task Scheduler XML file** with **all 3 triggers** already configured — then you just import and replace your path. It saves all the manual clicks.

Do you want me to do that?
