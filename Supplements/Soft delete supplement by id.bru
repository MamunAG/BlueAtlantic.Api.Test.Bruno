meta {
  name: Soft delete supplement by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/supplements/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
