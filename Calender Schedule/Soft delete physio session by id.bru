meta {
  name: Soft delete physio session by id
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/api/v1/calender-schedule/:id
  body: none
  auth: bearer
}

params:path {
  id: 16
}

auth:bearer {
  token: {{token}}
}
