meta {
  name: Permanently delete physio session by id
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/physio-sessions/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
