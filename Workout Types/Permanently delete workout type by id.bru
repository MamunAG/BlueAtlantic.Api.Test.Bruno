meta {
  name: Permanently delete workout type by id
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/api/v1/workout-types/:id/permanent
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
