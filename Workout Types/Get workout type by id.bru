meta {
  name: Get workout type by id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/workout-types/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
