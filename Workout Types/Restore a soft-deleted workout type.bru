meta {
  name: Restore a soft-deleted workout type
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/workout-types/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{token}}
}
