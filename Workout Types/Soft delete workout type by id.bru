meta {
  name: Soft delete workout type by id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/workout-types/:id
  body: none
  auth: bearer
}

params:path {
  id: 1
}

auth:bearer {
  token: {{token}}
}
