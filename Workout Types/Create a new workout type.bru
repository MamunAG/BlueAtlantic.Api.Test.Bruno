meta {
  name: Create a new workout type
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/workout-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "test222",
    "description": "des",
    "durationMinutes": 45,
    "status": "active"
  }
}
