meta {
  name: List workout types with pagination and filters
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/workout-types?status=inactive
  body: none
  auth: bearer
}

params:query {
  status: inactive
  ~page: 1
  ~limit: 10
  ~name: 
  ~search: 
  ~minDuration: 
  ~maxDuration: 
}

auth:bearer {
  token: {{token}}
}
