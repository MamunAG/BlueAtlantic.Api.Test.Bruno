meta {
  name: Soft delete a role by ID
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/roles/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
