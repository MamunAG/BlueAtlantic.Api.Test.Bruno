meta {
  name: Restore a soft-deleted role
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/roles/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
