meta {
  name: Restore soft-deleted inventory unit
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/api/v1/inventory-units/:id/restore
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
